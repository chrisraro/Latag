/**
 * `/` is Home. The screen itself lives in `home.tsx` so the file name says what
 * it is; this re-export keeps the tab's route name (`index`) — and every deep
 * link to the app's root — exactly where F1 left it.
 */
export { default } from "./home";
